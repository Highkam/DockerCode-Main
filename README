Las instruciones para ejecutar el contenedor de forma adecuada son las siguentes:


Primero clonar el repositorio:
Para clonar el repositorio, usa el comando 
`git clone https://github.com/Highkam/DockerCode-Main`.

Navega al directorio:
`cd DockerCode-Main`

Ejecuta el comando Build:

`docker build -t main .`

Ejecuta el comando Run
`docker run -it main`


```sh
node app
```
